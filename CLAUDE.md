# daifugo-online

友達とスマホのブラウザで遊べる大富豪（President系トランプゲーム）。
Cloudflare Workers + Durable Objects でルームごとの対戦状態をリアルタイム同期する。

## 構成

```
wrangler.jsonc      Cloudflare設定（Durable Objects + 静的アセット配信）
src/index.js        Worker本体。DaifugoRoom (Durable Object) が1部屋=1インスタンス。
                     ゲームロジック（役判定・進行・CPU思考）はここに全部ある。
public/index.html   HTMLシェル。スタイル(CSS変数によるライト/ナイトテーマ)のみ。
public/app.js        フロントエンドのロジック（画面描画・WebSocket通信）。
public/rules.js       ルールカタログ。src/index.js と public/app.js の両方が
                     この1ファイルをimportして使う「ルール定義の唯一のソース」。
```

## ルールを追加/変更するとき

**必ず `public/rules.js` の `RULE_CATEGORIES` を編集する。** ここに1項目追加するだけで：
- デフォルト値（`buildDefaultRules()`）に自動で入る
- ロビー画面の設定パネルに自動でチェックボックス/セレクトが出る（カテゴリ単位で折りたたみ表示）
- サーバー側は `rules.xxx` でその値を参照できる

ルールの「効果」自体（例：8を出したら場が流れる、等）は `src/index.js` の
`applyPlay()` 内に条件分岐として実装する。`rules.js` にキーを足しただけでは
UIには出るが実際のゲーム挙動には反映されない点に注意（判定ロジックは別途実装が必要）。

判定ロジックの実装状況は `applyPlay()` 内のコメント（革命系／数字系／Joker・返し技／
縛り／上がり禁止 の各ブロック）を参照。

## デプロイ

GitHub連携（Cloudflare Workers Builds）済み。`main`ブランチにpushすると自動でビルド・
デプロイされる。手動デプロイしたい場合のみ：

```bash
npx wrangler deploy
```

`wrangler.jsonc` の `compatibility_date` は未来日付にするとデプロイが失敗する
（`[code: 10021]`）。今日以前の日付にしておくこと。

## データモデル（room オブジェクト）

Durable Objectの `this.room` がゲーム状態そのもの。主なフィールド：

- `players[]` : { id, name, hand[], handCount, finished, finishOrder, isCPU, foul }
- `order[]`   : 席順のプレイヤーID配列（ゲーム開始時に確定）。`rules.seatShuffle` が
                `"every"` なら毎回シャッフル、`"first"` なら初回の並びを `seatOrder` に
                覚えて引き継ぐ。**`currentTurnIndex` は `order` の添字**なので、
                開始プレイヤーを決めるときは必ず `order.indexOf(id)` から引くこと
                （`players` の添字と混同しない）
- `field`     : 場に出ているカード。`{kind:'set'|'stairs'|'joker', rank/startRank, count, cards[]}` か `null`
- `pile`      : 場が流れずに続いている間に出た手 `[{cards[], by}]`（最大 `PILE_MAX` 手）。
                **クリアは `clearField()` に入れてある**ので、場が流れる経路（8切り・全員パス・
                スペ3返し等）は全部そこを通れば勝手に空になる。`r.field = null` を直接書くと
                pile が残って場に幽霊が出るので書かないこと。画面では最後の1手だけ大きく
                （`.pile-g.now`）、それ以前は小さく薄く出す（`.pile-g.old`）
- `rules`     : `public/rules.js` の `buildDefaultRules()` から生成、ホストが `setRules` で変更
- `pending`   : 7渡し/10捨て/Qボンバーなどの保留アクション。`{type, playerId, count}`
- `classes`   : ゲーム終了後の階級マップ `{playerId: "大富豪"|...}`（都落ち判定に使う）

### 反則上がり（forbidden）の扱い

**手は止めない。** 禁止カードで上がろうとしても弾かず、そのまま上がらせて `foul: true`
を立て、順位だけ下位に回す（弾くと、その札しか持っていない人が永久に打てず進行が止まる）。

反則した順を `foulOrder`（room の `foulSeq` を採番）に控え、`checkGameEnd()` で
「無事故の人 → 反則した人」の順に並べ直す。反則者どうしは `foulOrder` 昇順、
つまり**あとに反則した人ほど下位**になる。

判定箇所は2つある。**両方を直さないと片方だけ素通りする**：

1. `applyPlay()` … 場に出して手札がゼロになった場合
2. `resolvePending()` … 7渡しで最後の札を渡した／10捨てで最後の札を捨てた場合。
   元の一手（7 / 10）で上がったのと同じとみなし、`forbidden.seven` /
   `forbidden.ten` で反則にする

### 手札ゼロなのに「上がっていない」状態を作らないこと

Qボンバーは他人の手札を抜くので、抜かれた側が手札ゼロになりうる。ここで
`finished` を立てないと、その人は**出す札もなく、場が空だとパスも拒否される**ため
手番が回った時点で進行が永久に止まる。`resolvePending()` のボンバー処理で
手札ゼロになった人はその場で上がり扱いにしている（自分の一手ではないので反則にしない）。

### ログの順序

`applyPlay()` では「出した手」のログを**上がり処理より先**に積む。逆にすると
反則の通知が出した手のログに上書きされ、画面は最新1行しか出さないため
プレイヤーが反則に気付けない。

各プレイヤーには自分の手札しか見えない（`sanitize()` でWebSocket送信時にマスク）。

テストモードでも他人の手札は見せない。**例外は「いま手番の人」の手札だけ**で、
これはホストが全員の代わりに操作するために必要だから（`actingId()` が
保留アクション → カード交換 → 通常の手番 の順に判定する）。**送っていないものは
見えない**ので、画面側で隠す作りにはしないこと。

## CPU

`decideCPUMove()` が思考ロジック。同ランクの組み合わせしか使わない簡易版（階段・
Joker代用はしない）。Durable Objectの `alarm()` を使って1秒程度の間を置いて着手する。

## 開発者モード・テストモード

**URLに `?dev=1` が付いているときだけ**、ロビーに「開発者メニュー」が出る。
判定はフロントの `IS_DEV` 定数（`location.search` を見るだけ）。

**端末に記憶させないこと。** 以前は `localStorage` に覚えさせていたが、通常URLを
開いたつもりでも開発者メニューが出続けて紛らわしいため取りやめた。SPAなので
部屋を作っても画面遷移は起きず、`?dev=1` はセッション中そのまま残る。

開発者メニューの中身：

- **開発部屋に入る**（スタート画面）… 部屋コードのやり取りを飛ばして固定の部屋
  `DEV_CODE`（"DEV0"）に直行する。サーバー側は `enter` メッセージで受け、
  **部屋が無ければ作り、あれば join と同じ扱い**にする。`create` は無条件に
  `this.room` を上書きするので、この用途に流用しないこと（先客の部屋が消える）
- **テストモード** … 2人からでも開始でき、誰の番でもホストが代わりに操作できる
  （`room.testMode`）。見えるのは手番の人の手札だけで、他人の手札は本番と同じく伏せる
- **ダミー席** … 自動着手しない空席（`isDummy: true`）。CPUと違い `alarm()` の
  対象外なので、全員分を1画面で自分だけで動かせる。ダミー席が1つでもあると
  サーバー側で強制的にテストモードになり（`start` ハンドラ）、`rematch` 後も
  テストモードを維持する（連戦してもセットアップが崩れない）

CPU（`isCPU: true`）は本番機能なので、開発者モードでなくても誰でも追加できる。

## 身内ルーム（合言葉モード）

トップ画面の右下の薄い ♠（`.corner-mark`）→ 合言葉 → 部屋コードを打たずに固定の部屋へ入る。
一般の人が使う4文字コードの部屋とは**別系統**で、共存する。

- 合言葉は**1人1本**。全員分が同じ部屋に着く。誰の合言葉で入ったかが分かるので、
  漏れたらその1本だけ停止・再発行すればよく、他のメンバーには影響しない
- 合言葉の台帳は `PrivateRegistry`（Durable Object・インスタンスは `idFromName("registry")` の1つだけ）
- 管理画面は ♠ → 「管理」→ マスターキー。キーは Cloudflare のシークレット `DAIFUGO_MASTER_KEY`
  （ローカルは `.dev.vars`）。**ソースにも localStorage にも保存しない**

### 部屋コードをブラウザに返さないこと

**ここが設計の要。** 部屋コードを返すと、身内ルームのロビーを写真に撮られただけで
通常の「部屋コードで参加」欄から入れてしまい、合言葉が意味を失う。そのため：

```
① POST /api/gate {pass}     → 合言葉を照合し、通行証を返す（部屋コードは返さない）
② WS /api/private/{通行証}/ws → Worker が通行証から部屋を特定し、使い捨てて転送
```

通行証は32文字・60秒・1回きり。Worker は転送時に `X-Daifugo-Member`（base64のJSON）を付ける。
**DO には Worker からしか到達できないので、このヘッダが付いていること自体が合言葉の証明**になる。
身内ルームの DO はこのヘッダの無い接続を拒否し、逆に一般の部屋はこのヘッダ付きの接続を拒否する。
ヘッダに日本語を直接入れると仕様違反になるので base64 で運ぶこと。

### DO 名の名前空間を分ける

`create` は既存の部屋を無条件に上書きするため、身内ルームを4文字コードにすると、
一般の人が「部屋を作る」でたまたま同じコードを引いた瞬間に消える。

| | DO 名 | 到達経路 |
|---|---|---|
| 一般の部屋 | `ABCD`（4文字） | `/api/room/{code}/ws` |
| 身内ルーム | `priv:{12文字のid}` | `/api/private/{通行証}/ws` のみ |

`priv:` はコロンを含むのでルート正規表現 `[A-Za-z0-9]{4}` に絶対に一致しない。

### 席のIDはサーバーが決める（`room.you`）

身内ルームでは `msg.playerId`（ブラウザの自称）を信用せず、登録メンバーのIDを使う。
そのため**クライアントは自分のIDを知らない**。`sanitize()` が `you: forPlayerId` を必ず添え、
クライアントは state を受けたら `state.playerId` をそれに合わせる。
**これが無いと自分の手札もホスト権限も見つからず、ゲーム画面が受け皿画面に落ちる。**
`resetToTitle()` で端末のID（`daifugo-pid`）に戻す。

### 常設であること

`leave` の「人がいなくなった部屋は消す」処理は `r.private` のとき**通らない**。
これが「翌日また同じ部屋・同じルールで集まれる」の要。`disband`（解散）も身内ルームでは拒否する
（消すのは管理画面からだけ）。中断（`abort`）は今まで通り使える。

### 総当たり対策

`PrivateRegistry` が IP ごとの失敗回数を持ち、5回で60秒、以降は倍々（上限あり）待たせる。
**掃除の基準は `until` ではなく `at`（最後に外した時刻）にすること。** `until` で掃除すると、
まだ待たせる前（`until=0`）の記録まで消えて回数が永久に溜まらない。
1時間空けば数え直す。締め出された人は管理画面の `clearLocks` で解除できる。

## 手札の表示（重ね表示）

手札は**何人対戦でも必ず1行**。横スクロールさせないため、カードを重ねて幅に押し込む。

- 重なり量と数字のサイズは CSS では決まらない。`app.js` の `layoutHand()` が
  描画後に `.hand-row` の実測幅から計算し、`--hand-m`（margin-right）と
  `--hand-fs`（数字のフォントサイズ）を流し込む
- そのため `render()` は `paint()`（HTML生成）→ `layoutHand()` の2段構え。
  **画面を描く処理を増やすときは `paint()` の中に書く。** `render()` を
  書き換えて `layoutHand()` の呼び出しを外すと、手札が重ならず横に溢れる
- カードの数字とマークは**左端に寄せてある**（`.hand-row .card`）。重なっても
  左端の帯だけは残るので、そこだけで見分けられるようにするため。中央寄せに
  戻すと、隠れたカードが何か分からなくなる
- 選んだカードは浮き上がるだけで**手前には出さない**（`z-index` を上げない）。
  手前に出すと右隣のカードが完全に隠れてしまうため

## 抜け出せなくなる状態を作らないこと

「進まない・戻れない」が一番タチが悪いので、**どの画面にも必ず出口を置く**。

- 全画面の隅に `menuBtn()`（☰）→ `menuOverlay()`。中身は
  **対戦を中断してロビーへ**（`abort`／ホストのみ）、**部屋を解散**（`disband`／ホストのみ）、
  **タイトルに戻る**（誰でも）。中断・解散は取り返しがつかないので確認をはさむ
- `leaveRoom()` は、ロビーなら `leave` を送って席を空け、**対戦中は接続だけ切る**
  （サーバーは対戦中の退出を受け付けない）。`playerId` は端末に残るので入り直せば席に戻れる
- `disband` はサーバーが全員に `{type:"disbanded"}` を投げてから部屋を消す。
  クライアントは `resetToTitle()` でスタート画面へ
- **`paint()` の末尾には「どの画面にも当てはまらないとき」の受け皿がある。**
  これが無いと、席が消えた等で真っ白＋操作不能になる。画面分岐を足すときも消さないこと
- `join` / `enter` 時に**ホストが繋がっていなければ、入ってきた人にホストを渡す**。
  放置された部屋（特に `DEV0`）は、これが無いと誰も中断・解散できなくなる

## 保留アクションのポップアップ

7渡し / 10捨て / Qボンバーは通常と違う操作なので、見落とさないようポップアップで出す。
**2種類あり、使い分けを間違えると操作不能になる**：

- **Qボンバー** … 数字を選ぶだけなので画面中央のモーダル（`.overlay.overlay-center`）。
  背景を暗くして塞いでよい
- **7渡し / 10捨て** … **手札から札を選ぶ操作が要るので、画面を塞ぐモーダルにはできない。**
  手札パネル（`.panel-bottom`）を基準に、そのすぐ上へ浮かせる（`.pop-above`）。
  `.panel-bottom` の `position:relative` が位置の基準なので外さないこと

## よくある作業

- 新しいローカルルールを足す → `rules.js` にエントリ追加 → `applyPlay()` に判定を実装
- CPUを賢くする → `decideCPUMove()` を拡張
- 見た目を変える → `public/index.html` の `<style>` (CSS変数で統一)
